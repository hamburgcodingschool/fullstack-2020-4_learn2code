// Arrays are objects, so they have properties and methods

let values = [13, 15, 18, 19, 20, 200, 45, 78];

// property of the array object: length
console.log(values.length);

// method of the array object: push
values.push(79);

console.log(values);
console.log(values.length);
